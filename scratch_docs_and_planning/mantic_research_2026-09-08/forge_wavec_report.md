## Code Review Results

### Verdict: NEEDS WORK

31 findings survived verification (39 filed; the verifier and this pass merged 12 duplicates). 27 enter the plan (5 important, 22 minor), 4 are declined. Nothing critical. The per-bin numeric path itself is sound (server-rule acceptance was replicated over ~16,800 constructed cases with zero violations); the important defects are in the supply probe's request shape, one pre-existing spread guard that forfeits a whole question, one missing prompt sentence, and one untested arithmetic claim.

### Stated concerns
- **Metaculus byte-identity:** addressed, with one tightening (F16). `elicit_per_bin` has exactly three callers (`forecaster_runners.py:328` on the numeric question, `:380` on the epoch view of a date question, `aggregation_pipeline.py:512` via `numeric_view`), and it requires `question_platform(question) in PMF_ELICITATION_PLATFORMS` (Mantic-only) AND an outcome-space grid AND at most 31 bins, so it is False for every Metaculus question and for every 201-point or 200-bin Mantic grid. `_numeric_combine_strategy` returns the caller's strategy on the false side. The completeness reviewer's SHA-256 comparison of six percentile renders (numeric-201, Metaculus discrete-22, date-201, binary, MC, Mantic discrete-200) at a7f438a versus e1343db was identical. The one Metaculus-visible addition is the optional trailing `method=` field on NUMERIC_AGGREGATE, by design. F16 removes the only place the axis half and the elicitation half of the template could disagree. Separately, F5 deliberately changes the Metaculus request of the read-only supply-probe script; see the last concern.
- **Server-rule acceptance of the per-bin path end to end:** addressed, with two test gaps in the plan (F1, F26) and one raise-out-of-the-seam issue found (F3). The concern-2 reviewer replicated the platform validator byte for byte and ran every corpus coarse grid plus synthetic 2 to 31-bin grids in all four bound shapes, linear and zero_point axes, k=1/2/3 mean pools, the Mantic tail floor and the real `_aggregate_predictions` seam: zero violations, no ramp fire, `is_date` preserved, `declared_percentiles` bit-identical to `get_cdf()`. The min-step worry is structurally impossible (members leave every step at min_step + 1e-9, and the mean of values at least X is at least X). But `compute_spread` raises on a grid of 3 bins or fewer with an open bound carrying more than 10% mass, and that raise escapes `route_after_forecasts` and forfeits the question (F3, pre-existing, made likelier by Wave C).
- **Bin-label and key matching:** issue found, F2 and F19. Center-style grids get no key sentence, so on the 5 of 46 coarse Mantic center grids with step != 1 a key like "5000" can be read as a left edge and a belief lands one bin low (F2). The reserved-key map in `_pmf_from_pairs` carries dead openness flags (F19). Everything else is pinned: round-trip of every label style, the post 650 off-by-one (`test_the_bin_list_comes_from_the_grid_labels_not_the_displayed_maximum`), a reserved key on a closed bound failing, the every-key rule refusing a truncated block. The runner's `pmf_grid(view)` (`forecaster_runners.py:431`) and `pmf_prompt`'s `pmf_grid(view)` (`prompts.py:1916`) see the same object: the runner passes the epoch view, which is not a `DateQuestion`, so `pmf_prompt` passes it through unchanged. Duplicate labels on sub-day bins are declined (F29): unreachable on Mantic's one-day minimum step, and systematic duplication already fails the sum check.
- **Telemetry as data contracts:** addressed, with two documentation-contract fixes (F6, F7). The `scripts/telemetry/markers.py` diff adds `(?:\s+elicitation=(?P<elicitation>\S+))?` and `(?:\s+method=(?P<method>\S+))?` as optional trailing groups only; the EXTRACTION_RUNG regex is unchanged (docstring only); `method=` is pinned under STACKING and MEAN (`tests/test_aggregation_pipeline.py:1099-1109`) and on the single, median and stacked paths; no marker, field, status token or skip reason is renamed or re-spelled anywhere in the diff. The prose behind `fallback_median` (`comment/markers.py`) and behind `method=mean` (`member_forecast.py`, the telemetry registry) now over-claims; F6 and F7 correct the prose without touching a token.
- **Timing, deadline, retry, fallback and fail-shut on the per-bin path:** addressed for the runner; the aggregation-stage raise is F3. `_run_pmf_forecast` uses the same `invoke_with_broad_retry(..., wall_timeout=FORECASTER_SOFT_DEADLINE, label=...)` with the labels `forecaster_numeric` / `forecaster_date`, adds no retry, and is wrapped by `_forecaster_with_soft_deadline` like every runner. `_gather_results_and_exceptions` (`forecaster.py:606-625`) reads `task.exception()` for ANY BaseException, records the drop through `classify_raised_drop_cause` (a `ValueError` or `RuntimeError` from `extract_pmf` / `build_pmf_distribution` lands as `error_other`), and the survivors publish. No new exception type escapes. F28 (no MEMBER_FORECAST line for a member dropped inside `build_pmf_distribution`) is declined: the drop is still in FORECASTER_DROPS and the LLM OUTPUT dump.
- **The supply probe:** issue found, F4, F5, F20, F21. The tokenless Mantic mode classifies nothing because `with_cp=true` is sent only under a token (F4, verified live by four independent reads). F5 adds `with_cp` to the Metaculus request too, which DELIBERATELY changes the Metaculus request shape the review was asked to confirm unchanged; the Wave C diff as shipped did leave it byte-identical (the pins at `tests/test_supply_probe_mantic.py:488-530` hold at HEAD), so F5 is a separate, token-verified improvement to a free script, and the operator may say "skip F5". Non-issues with evidence: no LLM call and no publish anywhere in the script; the token is never printed (pinned at `:443-444`); paging stops on the first short page despite Mantic's always-populated `next` (pinned at `:305-312`); the per-hour table is a pure function of the rows (`test_the_metaculus_mode_gets_the_same_table`). The fixture `tests/data/mantic_series1_resolved_post_500_public_2026_09_09.json` holds no `Authorization`, `token`, `my_forecasts` or `subscriptions` key (a public detail payload, no secret), but it is a DETAIL payload while the mode consumes LIST pages; F4 re-records it as a list page.

### Verification gates
- **live-run: make run_mantic_one POST=651 (per-bin elicitation, paid, publishes):** The whole per-bin path (pmf_prompt render, the `pmf` block a real frontier model actually writes, the extraction ladder's block/repair/LLM rungs on that text, the floor blend, and the server accepting the 13-value CDF) has never run against a live model or the live Mantic API. Automated tests use synthesized rationales. docs/operations.md already carries the verification checklist (13-value CDF with cdf[0]==0.0 and cdf[12]==1.0, the three weekend bins at the platform minimum, three `MEMBER_FORECAST ... qtype=date ... elicitation=pmf` lines with 14-entry vectors, one `NUMERIC_AGGREGATE ... method=mean`, ISO dates in the comment). About $3 on the operator's personal keys, and it publishes to competitions.mantic.com, so it is operator-gated per the AGENTS.md cost rule: confirm the smoke ran and passed that checklist, or waive it.
- **cli: scripts/supply_probe.py --platform mantic (make supply_probe_mantic):** Free and read-only, but the Mantic mode has only been exercised against captured dict fixtures. The load-bearing assumption is that the PUBLIC posts LIST page (no token, no with_cp) carries `question.aggregations.recency_weighted.score_data.disagreement_forecasts.forecasts[]`; the committed fixture `tests/data/mantic_series1_resolved_post_500_public_2026_09_09.json` is a single-post payload, not a list page, and the corpus claim behind the design lives in a gitignored scratch file. One real run confirms the list page answers, that author id 81 classifies as `forecast` rather than a false forfeit, and that paging terminates on Mantic's always-populated `next`.
- **plan-item: operator decision 3, the linear opinion pool for per-bin members (plan section 2.6):** The plan lists this as an open operator decision because it changes what Mantic publishes on per-bin questions and revives, for per-bin members only, the median-over-mean choice the repo previously benchmarked and rejected. The code implements the plan's recommendation (`_numeric_combine_strategy` returns MEAN when `elicit_per_bin`), and the commit message records it as decided, but there is no operator sign-off in evidence.
- **plan-item: operator decisions 1 and 2, PMF_ELICITATION_MAX_BINS = 31 and the Mantic-only platform gate (plan section 2.2):** Both were listed as open operator decisions. The code ships the recommended values (31 bins; `PMF_ELICITATION_PLATFORMS = frozenset({PLATFORM_MANTIC})`) and the commit records them as decided. Both are one-constant changes and the Mantic-only default is the conservative one, so this is a confirm-or-adjust signoff rather than a code problem.
- **plan-item: plan section 2.8's neutral rewording of the three shared percentile-worded template lines:** The plan required step (3) "shift percentiles", step (7)'s small-delta check and anchor-adherence clause, step (8)'s width bullet and `_MULTI_RESOLUTION_CONTINUOUS_RULE`'s tail to be reworded neutrally "on both platforms (a config-era change already covered by the Wave C merge)". The implementation instead made them elicitation slots whose percentile fills are byte-identical to today's text. I verified the outcome directly: rendering the numeric-201, Metaculus discrete-22, date-201, binary, MC and Mantic discrete-200 prompts at a7f438a and at e1343db gives identical SHA-256 digests for all six. So the refactor is provably non-regressive, but the plan's neutral-wording item is not implemented, which is the operator's call since it is a prompt-bytes / config-era decision.
- **plan-item: plan section 2.4's flat `_PMF_PROB_SUM_TOLERANCE` of 0.02:** The plan specified the multiple-choice ballot's flat 0.02 for both the `PmfStructured` schema check and `_make_validate_pmf`. The code ships `pmf_prob_sum_tolerance(key_count) = max(0.02, 0.005 * key_count)` instead, accepting up to 0.165 of sum drift on a 33-key block. The rationale (two-decimal rounding drift per key, and a paid parser call plus a possible dropped member if the flat bound fails an honest block) is recorded in docs/value_extraction.md and is defensible; it needs a confirm because it loosens a validation bound the plan and its adversarial review both reasoned from. See the related finding on the two stale "2% of 1.0" receipts.
- **Metaculus group-post and multiple-choice list payloads under with_cp=true:** Neither the summer-futureeval-2026 pages nor a 60-post site-wide resolved page held a group post, so the list-versus-detail parity behind both findings is verified only for single-question posts. If a group post's list entry truncates its sub-question array, the supply probe's detail sweep must stay as the fallback (my recommended fix already leaves it in place) and the collector change needs that case checked first.
- **Per-bin elicitation core (numeric/pmf_grid.py, numeric/pmf_cdf.py, value_extraction.extract_pmf, prompts.pmf_prompt, forecaster_runners._run_pmf_forecast):** No efficiency finding at or above the reporting bar: every array is at most 33 entries (the elicit_per_bin gate caps bins at 31) or the 201/2001-point grid, all work is numpy or dict lookups dwarfed by the LLM round trip, and the path removes an LLM call (the discrete-integer vote) rather than adding one. The only redundancy I found is pmf_grid(view) being computed twice per forecaster call (forecaster_runners.py:431 and again inside prompts.pmf_prompt at prompts.py:1916), which is microseconds and falls under the lens's micro-optimization exclusion; it is worth a mention only as a DRY item. The new tests are fast (543 tests across the six new/changed pmf and Mantic files in 2.6 s; time.sleep is patched in the paging tests).
- **A live Metaculus run (published comment body and submitted CDF):** Verified only through the render comparison against the parent commit, the mocked-LLM e2e suite and the full free test suite. A paid Metaculus run would be the only way to confirm the published comment and the submitted payload end to end, and the repo's cost gate puts that decision with the operator, not a reviewer. Nothing in the diff touches metaculus_bot/comment/, publish_hardening.py or publish_gate.py, so I have no reason to think a live run would differ.
- **live Mantic (competitions.mantic.com) continuous_cdf acceptance:** Verified only against the replicated validator (tests/pipeline_test_helpers.assert_server_accepts_cdf and numeric/pmf_cdf.validate_grid_cdf, both transcribed from questions/serializers/common.py in the open-source Metaculus backend) plus the forecasting-tools client's own monotone and [0,1] checks. No live submission was made: a --mode mantic or --only-posts run spends the operator's personal keys and publishes, so it is behind the repo's cost gate. If the backend Mantic forked has diverged from the transcribed rules, no test in this repo would see it.
- **day/week date grids whose bins are not whole calendar days:** numeric/pmf_grid._label_bins labels a day- or week-granularity grid with format_epoch(edge, granularity), i.e. the ISO date alone, with no check that the labels come out distinct. A day-granularity Mantic question whose bins were narrower than 24h would produce duplicate labels, and value_extraction._pmf_from_pairs would then assign the same probability to both bins and fail its own sum check, dropping every member and forfeiting the question. I could not rule this in or out: the recorded corpus (tests/data/mantic_cdf_grids_2026_09_08.json) carries only bin counts and bound flags, not bin widths, and the one real day grid I could exercise (post 651, 12 bins over 12 days) is clean. Confidence that the shape is reachable is well under the reporting threshold, so it is recorded here rather than as a finding.
- **live per-bin elicitation on a real Mantic coarse grid:** End-to-end proof that three frontier models actually return all N+2 grid keys spelled verbatim (the correlated-drop risk the deterministic label matching creates) needs a paid Mantic run at about $2.60/question, which the repo's cost gate reserves for the operator. Everything above was verified against the code, the shipped fixtures and in-process builds of the pipeline.
- **The token-present Mantic path (with_cp=true putting my_forecasts on the list page):** No MANTIC_TOKEN available to this session, so I could not confirm that with_cp=true adds my_forecasts under authentication. Indirect support only: the recorded corpus fixture tests/data/mantic_preseason2_posts_2026_09_08.json carries both my_forecasts and aggregations on its questions, consistent with an authenticated with_cp list read.
- **Which account is the one systematically missing from every disagreement_forecasts snapshot:** Not resolvable from public endpoints in reasonable time: no leaderboard or per-question forecaster-roster endpoint answers (both /api/projects/3/leaderboard/ and /api/leaderboards/?project=3 return 404), and the missing account never appears in any of the 100 snapshots so it cannot be identified by difference. The off-by-one itself is measured and certain; the mechanism is not.

Triage notes on the gates (none promoted to findings): the second gate's premise ("no token, no with_cp carries the snapshot") is false, and F4 is the fix, so run `make supply_probe_mantic` AFTER step 1 lands. The group-post parity gate is largely discharged: the verifier for F11 confirmed list/detail parity live for all four single-question types and a group post's members, with only a post-level `subscriptions` key detail-only. The `pmf_grid(view)`-twice aside stays unpromoted: removing it means threading a `PmfGrid` through `pmf_prompt`'s public signature for microseconds, which leaves the code worse. The day/week-grid gate is the declined F29. The token-present path claim (with_cp adds my_forecasts under a token) was confirmed live by the F31 reviewer on preseason-2 post 650.

### Important (should fix)
- **[F1]** `metaculus_bot/numeric/pmf_cdf.py:183-189` Floor-blend max reduction pinned by no test (confidence: 100) — concurred: tests
  - Problem: every deficient cell in every test declaration is exactly 0.0, where the per-cell ratio collapses to sum(floors), so np.max, np.min and np.mean are indistinguishable; the np.min mutant survives 626 tests, and the fail-shut guard does not see an under-lifted cell because safe_cdf_bounds repairs it. HEAD uses np.max (verified this pass; the np.min aside in one verifier's reasoning does not describe the committed code), so this is a test gap, not a live bug.
  - Fix: a mixed-deficiency case (12-bin closed/closed, 0.0005 on bin 0, 0.0001 on bin 3, rest on bin 8) asserting the exact published values 0.001328334, 0.000932334, 0.000833334.
- **[F2]** `metaculus_bot/prompts.py:1619-1625` No key rule for center-style grids (confidence: 88) — concurred: completeness
  - Problem: `_PMF_KEY_RULES` has no `center` entry and the axis block never says what values a bare numeric key covers; on the 5 of 46 coarse Mantic center grids with step != 1 (post 558 at width 500, three at 0.1, one at 0.25) a left-edge reading puts the belief one bin low, a full log-score miss. The plan claims the grid sentence covers this; it does not.
  - Fix: add `"center": "Every key is the value at the centre of its bin; the bin covers half the stated width either side of that value."`, make the dict total, pin it, update docs/prompts.md:49.
- **[F3]** `metaculus_bot/spread_metrics.py:89-99` compute_spread raises and forfeits the question on a 3-bin open-bound grid (confidence: 90, pre-existing) — concurred: correctness, concern-5
  - Problem: `_key_percentile_values` rejects a CDF grid shorter than 5 points unless it spans P10-P90; a 3-bin grid has 4 points and more than 10% beyond an open bound breaks the span. Reproduced on both the per-bin and percentile paths. The raise runs before the per-type stacking gate and escapes `route_after_forecasts` bare, so stacking-off does not protect it; Wave C's date prompt actively pushes mass onto `above_range`, and a short-window Series 2 daily question is a 1-3 bin grid.
  - Fix: thread the question in and test for a grid positively, `len(model_pcts) == question.cdf_size`, keeping the ValueError for anything else; update the four direct test calls and add a 3-bin regression case.
- **[F4]** `scripts/supply_probe.py:495-496` Tokenless Mantic mode sends no with_cp and classifies nothing (confidence: 97) — concurred: correctness, concern-6
  - Problem: `with_cp=true` is the only thing that puts the public spot-time snapshot on the list page, but the probe sends it only under a token; verified live and unauthenticated (0 of 100 resolved posts carry the snapshot without it, 100 of 100 with it). The report then prints a hint telling the operator the opposite of the truth, the docs promise the behaviour the code lacks, and two tests pin the broken request.
  - Fix: rename `authenticated_list_params` to `list_params` and merge it on every page GET; flip the pins; add a no-snapshot-reads-unknown case; re-record the fixture as a real unauthenticated list page; fix docs/supply_probe.md and docs/operations.md.
- **[F5]** `scripts/supply_probe_platforms.py:142` Metaculus probe keeps the N+1 detail sweep the same commit removed for Mantic (confidence: 95) — concurred: efficiency
  - Problem: with the bot's own token, `with_cp=true` puts `my_forecasts` on 100 of 100 Metaculus list-page questions and omitting it drops it on all of them, which is exactly what forces one detail GET per closed or resolved post at 0.5 s spacing. Operator note: this deliberately changes the Metaculus request shape; the Wave C diff itself left it byte-identical, and "skip F5" keeps it that way.
  - Fix: add `with_cp` to METACULUS_PROBE's list params, keep `sweep_needs_detail_gets=True` as the fallback, rewrite the two four-param pins, update the "does not reliably carry" sentence.

### Minor (in the plan)
- **[F6]** `metaculus_bot/comment/markers.py:7-8, 59-63` `fallback_median` prose contract false for per-bin questions (confidence: 88) — concurred: concern-4
  - Problem: `_median_fallback` now pools per-bin members by the MEAN while publishing `STACKER_OUTCOME=fallback_median`, whose documented meaning is "MEDIAN aggregation was used"; reachable today via the wall-clock budget skip at stacking_route.py:170.
  - Fix: prose only, pointing at `NUMERIC_AGGREGATE ... method=` as authoritative; rename nothing.
- **[F7]** `metaculus_bot/member_forecast.py:20-25` `method=mean` documented as implying per-bin (confidence: 88) — concurred: completeness
  - Problem: `_combine_by_type` records `strategy.value`, so `method=mean` is also written for percentile members under an explicit MEAN strategy (ablation and benchmark harnesses, plain STACKING's base combine); `elicitation=pmf` on the member lines is the real discriminator.
  - Fix: reword the bullet here and in the telemetry registry's method comment.
- **[F8]** `metaculus_bot/numeric/pchip_cdf.py:169` Fourteen raw copies of the 0.001 open-tail rule beside the newly named `OPEN_TAIL_MIN_MASS` (confidence: 85, pre-existing) — concurred: reuse
  - Fix: import the constant from numeric/config.py into pchip_cdf.py, replace the literals in pchip_cdf.py, utils.py and pchip_processing.py (0.999 as `1.0 - OPEN_TAIL_MIN_MASS`), update the three prose sites. Byte-identical.
- **[F9]** `metaculus_bot/numeric/pmf_cdf.py:48` Docstring claims the ladder bounds the declared sum to 2% of 1.0 (confidence: 95) — concurred: correctness, memory, completeness, grounding, concern-5
  - Problem: the real bound is `pmf_prob_sum_tolerance(key_count) = max(0.02, 0.005 * key_count)`, 0.065 on post 651's 13-key grid and 0.165 at 33 keys; repeated in docs/numeric_pipeline.md:289 and the test docstring at tests/test_numeric_pmf_cdf.py:233. Four reviewers filed it; merged.
  - Fix: state the key-scaled rule in all three places, naming the helper; do not change the per-key constant.
- **[F10]** `metaculus_bot/numeric/pmf_grid.py:81-84` Third copy of the fixed-point render idiom (confidence: 80) — concurred: reuse
  - Fix: one docstring line naming `research/number_format.format_decimal_value`, the precision difference and the import-linter contract, mirroring `ts_render._fmt`.
- **[F11]** `metaculus_bot/performance_analysis/collector.py:78-100` Scoring pull GETs every resolved post individually (confidence: 85, pre-existing) — concurred: efficiency
  - Fix: page with `with_cp=true`, return the payloads, delete the loop; parity confirmed live for all four single-question types and a group post.
- **[F12]** `metaculus_bot/prompts.py:(whole file)` 2,425 lines, FUTURE.md records 1,691 (confidence: 85, pre-existing) — concurred: smells
  - Fix: do not split mid-feature (import cycle unless the shared continuous template moves too); update the FUTURE.md monolith bullet and name the seam.
- **[F13]** `metaculus_bot/prompts.py:1221, 1296, 1302, 1699` Reserved key names hardcoded as prose in four prompt constants (confidence: 85) — concurred: reuse
  - Fix: interpolate `PMF_BELOW_RANGE_KEY` / `PMF_ABOVE_RANGE_KEY`; rendered text byte-identical.
- **[F14]** `metaculus_bot/prompts.py:1232` Output rule says the per-bin minimum "is added to every bin", which the blend does not do (confidence: 88) — concurred: grounding, concern-2
  - Problem: the blend lifts only under-floor cells and scales the rest DOWN, total fixed at 1.0; the sentence contradicts its own sum-to-1.0 clause and invites pre-subtraction. docs/prompts.md:48 repeats it.
  - Fix: "a bin you leave at 0 is lifted to the platform's per-bin minimum (about {min_step} on this grid) for you and the rest scaled down to keep the total at 1.0."; pins survive.
- **[F15]** `metaculus_bot/prompts.py:1637` Log-spaced grid clause and timestamp key rule render on admitted questions but have no pin (confidence: 85) — concurred: tests
  - Fix: two renders in TestBinsAndBoundsBlock (zero_point kwarg on `_pmf_q`; a date grid with granularity "").
- **[F16]** `metaculus_bot/prompts.py:1867, 1890` Kind of the axis half decided by the caller while the elicitation half re-derives it (confidence: 85) — concurred: concern-1
  - Fix: `axis=_kind_axis(...)` at both sites; byte-identical.
- **[F17]** `metaculus_bot/structured_output_schema.py:459-475, 495-508` Two copies of the probability-dict validator (confidence: 90) — concurred: structure
  - Fix: one `_validate_probability_dict(v, *, label, tolerance)`; drop the dead `isinstance(key, str)`.
- **[F18]** `metaculus_bot/structured_parse.py:90-105, 180-190` Two parallel dispatch tables; a missed edit falls through to a second paid parse silently (confidence: 85) — concurred: reuse
  - Fix: one `{item_type: (wrapper, field)}` mapping and `getattr`; update three test assertions.
- **[F19]** `metaculus_bot/value_extraction.py:683-695` Dead openness flags in `reserved_closed` (confidence: 92) — concurred: correctness, grounding
  - Fix: module constant `_RESERVED_KEY_BOUNDS = {below: "lower", above: "upper"}`; same messages.
- **[F20]** `scripts/supply_probe.py:810-813` Preflight is the one platform seam still chosen by `platform.name ==` (confidence: 82) — concurred: smells
  - Fix: `base_url` field on PlatformProbe (posts_url derived), `verify_api_identity(platform.base_url)`; repoint the tests that patch `verify_metaculus_api_identity`. Reconciled with declined F30: this shape keeps the call-time module-name seam, and the token already goes to an import-time `POSTS_URL`, so vetting an import-time `base_url` is vetting exactly the token's host.
- **[F21]** `scripts/supply_probe_platforms.py:122-124, 163` Snapshot caveat generalises from one post and understates a 97% property (confidence: 85) — concurred: concern-6
  - Fix: prose with the measured figures (507 of 524 short by one; 0 on 12, +2 on 4; eleven named accounts, absences spread across all of them; PRESENT branch never exercised live). The reviewer's own "ten accounts, 100 of 100" figures were wrong; use the verifier's.
- **[F22]** `tests/mantic_fakes.py:95-97, 41` Dead `load_coarse_discrete_question` loader (confidence: 90) — concurred: tests
  - Fix: delete it and narrow the import; post 643's parse is already pinned in test_mantic_e2e.py.
- **[F23]** `tests/pipeline_test_helpers.py:421-443` New helpers duplicate five private `_heights` copies and one `_distribution_from_heights` (confidence: 90) — concurred: reuse
  - Fix: `cdf_heights` and `distribution_from_heights` in the shared module; delete the copies.
- **[F24]** `tests/test_numeric_config.py:81-86` Four `_count_question` builders and two `_on_mantic` copies encode the half-step convention (confidence: 95) — concurred: tests, reuse
  - Fix: one `make_count_question` plus `on_mantic` in tests/pipeline_test_helpers.py; no boolean type/host selectors; leave `_CorpusShape`, `_pmf_q` and test_utils' `_question` alone.
- **[F25]** `tests/test_numeric_pmf_cdf.py:53` Corpus filter restates the 31-bin threshold as a literal (confidence: 95) — concurred: reuse
  - Fix: filter on `PMF_ELICITATION_MAX_BINS`.
- **[F26]** `tests/test_numeric_pmf_cdf.py:438` Builder + mean pool + 5% tail floor composition (the modal open-bound live path) untested (confidence: 90) — concurred: concern-2
  - Fix: one parametrised case, preferably in tests/test_out_of_range_floor.py, asserting the floor moved, server acceptance, and declared-zero bins back at exactly min step.
- **[F27]** `tests/test_supply_probe_mantic.py:122-129` Fifth hand-rolled `requests.Response` poking `_content` (confidence: 82) — concurred: reuse
  - Fix: `tests/http_fakes.py` with `json_response` / `text_response`; repoint five sites.

### Declined (NOT in the plan; each is a decision the user may reverse by saying so)
- **[F28]** `metaculus_bot/forecaster_runners.py:449-461` No MEMBER_FORECAST line for a member dropped inside build_pmf_distribution (confidence: 90)
  - Why flagged: the percentile path emits the marker before its fail-shut raise; the per-bin path's guards run inside the builder, before the marker.
  - Why NOT fixing (verifier): every `_validated_declaration` raise is unreachable from this caller (extract_pmf already enforces the same rules) and `validate_grid_cdf` is a fail-shut check no input should reach; the drop is in FORECASTER_DROPS as `error_other` and the declaration is in the LLM OUTPUT dump. Splitting the build or a try/except-log-reraise for a bug-only path leaves the code worse.
- **[F29]** `metaculus_bot/numeric/pmf_grid.py:120` Duplicate day/week labels on sub-day bins would double a day's mass (confidence: 82)
  - Why NOT fixing (verifier): systematic duplication already fails the sum-to-1 check; the silent path needs exactly one sub-day straggler on a 'day'-granularity question, a shape the platform's granularity contract forbids and 46,500 real grids never produced. A raise for a shape the deployment cannot produce is a branch added for nothing (proportion ruling, 2026-09-04).
- **[F30]** `scripts/supply_probe.py:810-813` Preflight as a `verify_identity` partial on PlatformProbe (confidence: 82)
  - Why NOT fixing (verifier): a `partial` in a frozen dataclass binds the function at import time and breaks the six existing monkeypatches (the repo's patch-where-used trap); F20 fixes the same seam with a `base_url` field instead.
- **[F31]** `scripts/supply_probe_platforms.py:126-133` Token block shadows a snapshot naming the bot (confidence: 80)
  - Why NOT fixing (verifier): upstream's `request_user_forecasts` prefetch has no post-status gate and no end_time filter, so an empty block on a closed or resolved post means the account genuinely never forecast before close; the non-bot-token case is already disclosed by the identity hint, which the reorder would silence.

### Holistic notes
- Merged duplicates: F3 (correctness-0-1 + concern-5-13-0), F4 (correctness-0-0 + concern-6-14-0), F9 (correctness-0-2 + completeness-1-1 + grounding-6-1 + concern-5-13-1), F14 (grounding-6-2 + concern-2-10-1, one merged wording), F19 (correctness-0-3 + grounding-6-3), F24 (tests-5-1 + reuse-3-0).
- F1 versus a verifier aside: the grounding-6-2 verifier wrote that the blend "takes np.min ... where its own docstring says max". I read HEAD (`pmf_cdf.py:189`): it is `np.max`, and the working tree is clean at e1343db. The aside most likely observed the tests-5-0 verifier's mutant in the shared tree. F1's new test passes against HEAD as written.
- F4 + F5 ordering hazard: F4's claim that the Metaculus pins "keep passing unchanged" holds only in isolation. Metaculus has `token_required=True`, so once list params ride every GET and Metaculus gains `with_cp`, every Metaculus request carries `with_cp=true`; `TestMetaculusModeIsUnchanged` and `test_the_metaculus_platform_never_sends_with_cp` are rewritten once, in step 1.
- F20 versus F30 at the same line: compatible. The declined variant bound a function at import time; the accepted variant stores a URL string and still calls through the module-level name. `constants.py:283` calls `load_environment()` at import, so an import-time `base_url` honours `METACULUS_API_BASE_URL` exactly as the token's destination `POSTS_URL` already does.
- No finding carries a `verifierNote` or `validity: uncertain`; every FIX rests on a verdict on the merits.

### Execution plan
1. **`scripts/supply_probe.py`, `scripts/supply_probe_platforms.py`, `tests/test_supply_probe_mantic.py`, `tests/data/`, `docs/supply_probe.md`, `docs/operations.md`** — rename `authenticated_list_params` to `list_params` and merge it on every page GET; add `with_cp` to METACULUS_PROBE keeping the sweep as fallback; flip the Mantic pins, rewrite the two Metaculus four-param pins, add a no-snapshot-reads-unknown case; re-record the fixture as an unauthenticated list page and scan it; fix both docs (resolves F4, F5). Correctness first; the two findings share a field, a builder and the same pins.
2. **`scripts/supply_probe_platforms.py`, `scripts/supply_probe.py`, tests, `docs/supply_probe.md`** — `base_url` field (posts_url derived), `verify_api_identity(platform.base_url)`, drop the unused imports, repoint the preflight monkeypatches; replace the snapshot caveat prose with the measured figures (resolves F20, F21). Same files as step 1.
3. **`metaculus_bot/spread_metrics.py`, `tests/test_spread_metrics.py`** — thread the question into `_key_percentile_values`, positive `len == cdf_size` grid test, update four direct calls, add the 3-bin open-upper regression (resolves F3).
4. **`metaculus_bot/prompts.py`, `docs/prompts.md`, `tests/prompts/test_pmf_prompt.py`** — center key rule with a pin and total dict; interpolate the reserved keys; reword the output rule's mechanism clause; `_kind_axis` at both percentile call sites (resolves F2, F13, F14, F16).
5. **`tests/prompts/test_pmf_prompt.py`, `tests/prompt_builders.py`** — log-spaced and timestamp-rule renders (resolves F15).
6. **`tests/pipeline_test_helpers.py` and the eight test modules named in the plan** — `make_count_question` / `on_mantic`, `cdf_heights` / `distribution_from_heights`, delete the copies, filter on `PMF_ELICITATION_MAX_BINS` (resolves F24, F23, F25). Before steps 7 and 8 so the new tests use the shared factories.
7. **`tests/test_numeric_pmf_cdf.py`** — mixed-deficiency floor-blend case with the exact published values (resolves F1).
8. **`tests/test_out_of_range_floor.py`** — builder + mean pool + tail floor composition over ORACLE_BIN_COUNTS x open shapes (resolves F26).
9. **`metaculus_bot/numeric/pmf_cdf.py:48`, `docs/numeric_pipeline.md:289`, `tests/test_numeric_pmf_cdf.py:233`** — the key-scaled sum-tolerance rule (resolves F9).
10. **`metaculus_bot/numeric/pchip_cdf.py`, `numeric/utils.py`, `numeric/pchip_processing.py`** — `OPEN_TAIL_MIN_MASS` sweep over fourteen literals and three prose sites (resolves F8).
11. **`metaculus_bot/value_extraction.py`** — `_RESERVED_KEY_BOUNDS` module constant (resolves F19).
12. **`metaculus_bot/structured_output_schema.py`** — shared `_validate_probability_dict` (resolves F17).
13. **`metaculus_bot/structured_parse.py`, `tests/test_structured_parse.py`** — `_LIST_WRAPPERS` mapping (resolves F18).
14. **`metaculus_bot/comment/markers.py`, `metaculus_bot/member_forecast.py`, `scripts/telemetry/markers.py`** — the two marker prose contracts (resolves F6, F7).
15. **`metaculus_bot/numeric/pmf_grid.py`** — cross-reference line in `format_bin_value` (resolves F10).
16. **`metaculus_bot/performance_analysis/collector.py`** — page with `with_cp`, delete the per-post loop (resolves F11).
17. **`tests/mantic_fakes.py`** — delete the dead loader, narrow the import (resolves F22).
18. **`tests/http_fakes.py` (new) and five test sites** — shared `json_response` / `text_response` (resolves F27).
19. **`FUTURE.md:2197-2235`** — prompts.py at 2,425 and the split seam (resolves F12).
20. **Gate** — `make all`, then re-run the six-shape prompt SHA-256 comparison against a7f438a to prove the percentile renders stayed byte-identical.

### Filtered
- 0 finding(s) dropped by the verifier as refuted.
- 4 finding(s) declined: real, but the code would not be better afterwards, including fixes that would add mechanism for a case this deployment does not produce (listed above with reasons).
- 0 finding(s) in the plan by default rather than by verdict.

### Summary
- Findings: 0 critical, 5 important, 22 minor (all in the plan), 4 declined
- Overall: needs-work